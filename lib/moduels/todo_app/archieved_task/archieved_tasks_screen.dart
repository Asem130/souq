import 'package:flutter/material.dart';

class ArchievedTasks extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Archieved Task',style: TextStyle(fontSize: 25,fontWeight: FontWeight.bold),),
    );
  }
}
